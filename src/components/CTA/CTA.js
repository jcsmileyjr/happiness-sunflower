import './cta.css';

const CTA = () => {
    return(
        <main className="cta--container">
            <button className="cta--button">Get the App</button>
        </main>
    );
}

export default CTA;